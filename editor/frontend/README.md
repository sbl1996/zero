## Monster Editor Frontend

```bash
cd editor/frontend
pnpm install
pnpm dev
```

The dev server proxies `/api` requests to `http://127.0.0.1:8000` so start the FastAPI backend before running the UI.
